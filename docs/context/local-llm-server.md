# 社内 LLM 推論サーバー導入検討（旧 auto-memory: project）

> 出所: Claude auto-memory `project_local_llm_server.md`（type: project / originSession 4ba3c7b3…）を 2026-07-22 にリポへ移設。
> メモリ側は本ファイルへのポインタのみ。編集はここを真実源とする。

ユーザーは**社内設備として LLM 推論サーバー導入を検討**中（2026-07 時点、社長へ打診予定）。

**背景**: 自宅で 6900XT + Devstral(≈24B) を運用済み。夜間の単純解析・定型コーディング・ソース→仕様書生成・ローカル git housekeeping を回している。これを会社設備化したい。

**申請の筋（合意した位置づけ）**: 速度でも単一プロジェクトでもなく、**①IP機密性（IM-110 の回路図/NET/専有FW をクラウドに出さず社内完結）＋②夜間バッチ・社内文書生成の横断共用インフラ**。IM-110 は最初の実証ユースケース。

**運用モデル**: **Claude(私)=オーケストレータ、ローカル=下請け**。私が仕様＋受け入れ条件（build通過・host-check・差分範囲）を書いて渡し、成果を検証して取り込む。振れるのは機械的・境界明確・検証が安いタスク（Doc生成/定型雛形/ログ解析/検索）。correctness-critical（補正式・protocol両側契約・数値安定性）は振らない。

**ハード所感**: Strix Halo(Ryzen 395 AI MAX, 128GB) は大モデル低電力向きだが帯域律速で生成/prefill 遅い。速度優先なら 24GB級GPU(3090等)。メモリ高騰期ゆえ最小investで様子見推奨。予算感 **50〜60万円**。まず手持ち 6900XT で実証フローを回してから調達判断。

**How to apply:** 承認見込みが立ったら、ユーザーから「夜間タスクリスト作成」を依頼される予定（例: Adjust.c 全ハンドラの Doxygen 補完、protocol-rs232c.md からの試験項目たたき台、ログ集計スクリプト）。IM-110 の作業分担は `adjust-mode.md` 等の correctness-critical は私側に残す前提。
