# MLSS 計算 全式・補正式・パラメータ リファレンス

> 目的: MLSS の測定値算出に関わる **すべての計算式・補正式・パラメータ**を、実コードを真実源として 1 箇所に集約する。
> これをベースに調整機能（ゼロ調整/AD調整/校正）の仕様を確定・修正する。
>
> **真実源（この文書の抽出元）**:
> - プローブ: `IM-110_Probe/Core/Src/IM_110.c`, `Core/Inc/IM_110.h`（生 mV 正規化・ADC 調整）
> - 本体: `IM-110/Core/Src/IM_110.c`, `Core/Inc/IM_110.h`（ABS→1次演算→機差→スパン）
> - 仕様書: `docs/specs/IM-110プログラム仕様書.md` 付録A（式の記述）
>
> 各式に `file:line` を併記。コードと本書が食い違ったら**コードを優先**し本書を直す。
> 作成: 2026-07-20

---

## 0. 全体演算チェーン（上流 → 下流）

```
[プローブ MCP3424]                          [本体 IM_110.c]
 ADC digit
   │ ①生mV正規化 (ch別)
   ▼
 MCP3424_AD_mV[ch]  ──②移動平均(SADA)──►  UART送信(MS/MD)  ──►  ADC_mV[ch] / ADC_mV_ave[ch]
                                                                     │  Y0=[0]受光, Ref=[1]
                                                                     ▼ ③ A/Dゼロ補正 + 温度補正 + 対数
                                                                   MLSS_ABSS (ABS)
                                                                     ▼ ④ ベース (出荷時2次式×k_No / TR=累乗)
                                                                   FABSS
                                                                     ▼ ⑤ 機差補正 kiza … 【廃止】(§4)
                                                                     ▼ ⑥ スパン校正 (SP_A/B/C, 現場2/3点)
                                                                   MLSS [mg/L] → 表示
```

### ADC チャンネル対応表（真実源: adjust-mode-design.md §204 / protocol-rs232c.md §195-207 / adj_mode_ch）

| ch番号(1始まり) | code index | 信号 | 物理 MCP3424 | 備考 |
|---|---|---|---|---|
| ch1 | `[0]` | MLSS 受光 (Y0) | CH1 | LED PWM 対象 |
| ch2 | `[1]` | MLSS Ref | CH2 | LED PWM 対象 |
| ch3 | `[2]` | SS 受光 | CH3 (SEL3 LOW, PGA×1) | LED PWM 対象 |
| ch4 | `[3]` | SS Ref | CH4 | LED PWM 対象 |
| ch5 | `[4]` | ~~透視度~~ **廃止** | CH3 (SEL3 HIGH, PGA×2) | **2026-07-26 廃止**。透視度は ch3=`[2]` を直接使う（下記） |

> **【2026-07-26 確定】ch5 廃止 — 透視度は ch3 (`[2]`) を直接使う**
>
> ch5 は ch3 と同一物理チャンネル (MCP3424 CH3) を SEL3 で切り替え、IN− に 1V を与えた差動入力を PGA×2 で読むものだった。これを廃止し、透視度の受光 Y0 も SS と同じ ch3=`[2]` を用いる。理由:
>
> 1. **空中出力が 1000mV を下回ると破綻する**。ch5 = ch3 − 1V ゆえ、ch3 の空中出力が 1V を割ると差動が負に振れ、`ABS = log10(I(0)/(Y0−ADZR))` の分母が 0 以下になり `log10` が −inf / NaN を返す。実測の ch3 空中出力は 1339mV で、マージンは 339mV しかない。LED 経年劣化・光学窓の汚れ・個体差でこれを割る危険がある。
> 2. **PGA×2 の分解能利得 (62.5µV → 31.25µV) が、注入される系統誤差に埋もれる**。`ADC_CH4_VREF = 1000.0f` はハードコードだが、実際の 1V 基準は LM336BMX-2.5 (IC9) + R39/R40 分圧で作られ、ちょうど 1.000V である保証がない。正規化が **差ではなく比**（`出力 = 生mV × 1450 / ADC_Span`）なので、加算オフセットは打ち消されない。基準が 1% ずれれば 10mV の系統誤差となり、分解能利得 31.25µV の 300 倍以上になる。
> 3. **ch5 は LED PWM 調整の対象外**であり、空中 1700mV 正規化の枠組みからも外れていた（`ADC_Span[4]` は既定 1000 のままで `×1.7` という根拠のないスケーリングになる）。ch3 に統一すれば `AAD0` の空中 1700mV 正規化がそのまま効く。
> 4. SEL3 は通常の測定ループで駆動されておらず（`Set_SEL3()` の呼び出しは `SSF` コマンドハンドラのみ）、実機の `MDR` でも `[4]` は初期値 1000.0 のまま動かない＝実質サンプリングされていなかった。
>
> **影響**: 透視度のゼロ基準 `TR_ZR` と出荷時係数は ch5 スケール前提だったため、**透視度側の調整はやり直しが必要**。出荷時3点未取得の段階での変更のため、やり直しコストは実質ゼロ。
>
> **ch5 関連の調整機能は一旦無効化する**（`SADS,4` は NG、`AAD0` は ch0〜3 のみ発行、`SSF` による SEL3 切替は無効）。ハードウェア（TMUX4053 の SEL3 配線、1V_REF 回路）は残置。

- **LED PWM 調整の対象 = ch1〜4**（index `[0]`〜`[3]`, MLSS受光/MLSS_Ref/SS受光/SS_Ref）。
- **ADSpan 個別 span傾きの対象 = ch1〜4**（index `[0]`〜`[3]`）。**ch5 は廃止につき対象外**（2026-07-26。旧: ch1〜5）。
- MLSS で使うのは **ch1=`[0]`(受光 Y0) / ch2=`[1]`(Ref)**。
- 本書では以降「ch番号」はこの 1 始まり、「`[n]`」は code index を指す。

---

## A. プローブ / 本体 責務分界（設計原則, 2026-07-20 確定）

- **プローブ = AD 値(mV)をそのまま出力**する。補正・計算は **本体** で行う。
- 係数・パラメータのうち **プローブに関するものはプローブ側フラッシュに保存**するが、**補正/計算は本体**。
- **プローブ側の計算に使う調整パラメータは 2 種のみ**:
  1. **`ADC_Span[]`** … 空中出力を 1450mV にする**傾き(span)正規化**基準（第1領域 PARAM）。**2026-07-21 に ADZero オフセットから span 傾きへ変更**。
  2. **LED PWM duty (`LED_Out[0]`)** … 初期値 0.36（第1領域 PARAM）
- **オフセット ADZero（`生mV − ADC_Zero` の減算）は廃止**（2026-07-20〜21 の巻き戻し）。プローブ側計算は span 傾き一本。`ADC_Zero`/`SADZ` は残置・計算未使用。

改訂後のプローブ処理（実質）:
```
プローブ出力 mV = 生mV(digit×LSB) × ADC_SPAN_INI(=1450) / ADC_Span   （LED は duty で駆動するだけ）
本体側        : 出力mV → ABS → 4次式(Mode_CF) → 温度補正 → 機差kiza → スパンSP → 物理量
```
- 空中で `ADC_Span` = その ch の空中生mV を捕捉するので、空中出力 = 生mV × 1450 / 生mV = **1450mV**。原点を通る純粋な傾き調整。
- Mode_CF(4次) 等の**係数はプローブ統合ストア(係数ページ)に保存**するが、**適用計算は本体**（現状どおり）。

> 本原則により、以下 §1 の正規化式は「span 傾き `×ADC_SPAN_INI/ADC_Span`（ADC_SPAN_INI=1700）を採用し、ADZero 減算は撤去」に戻す（2026-07-20 の ADZero 一本化を巻き戻し、ターゲットを 1750→1700 に変更）。

## A-2. 真実源・同期・初期化モデル（2026-07-20 確定、ID-200 の 1Wire EEPROM 流用）

- **真実源 = プローブ flash**（全係数、**現場上書き分も含む**）。本体故障交換時も再調整不要／プローブ交換時は再調整。
- **本体EEPROMミラー**: プローブ領域と**完全に同一配置**＋ **プローブ個体ID** ＋ **最終更新日**（RTC由来）を保持。非権威キャッシュ。
  **同一なのは各32Bページの payload [0..30]**。ページ末尾 byte31 は媒体固有チェックサムで一致しない
  （本体EEPROM `eep_write_page` = XOR seed 0xFF / 統合ストア `store_page_xor` = seed 0）。本体は
  ミラー読出時に byte31 を統合ストア正準形へ復元してから検証する（`mirror_load_raw`。KI-2 の教訓）。
- **本体側は LED PWM / ADC_Span / ADC_Zero(残置) など第1領域パラメータも読み出す**（プローブ個別調整値の確認用に共通領域として保持。正規化計算自体はプローブ側）。
- **プローブ個体ID は既存コマンドで実装済み**（新規不要）: 書=`SID,N`（uint32 10進）／ 読=`RPP` ダンプ中の `Probe ID:` ／ 永続=`WPP`。
  `Probe_ID`(`uint32_t`) は第1領域 PARAM に格納。本体ミラーにこの ID を持たせて突合する。

**起動時 初期化（3層 + ID 判定）**:
```
(1) 変数に既定値をセット
(2) 本体EEPROMミラー（同一配置＋プローブID＋最終更新日）から読み出し上書き
(3) 判定:
    プローブID ≠ ミラーID            → 必ずプローブから読む（別個体。=1Wireのセンサー製造番号に相当）
    プローブID = ミラーID かつ 日付[プローブ ≥ 本体] → プローブから読む（プローブ=真実源、同一もプローブ優先）
    プローブID = ミラーID かつ 日付[本体 > プローブ]  → 本体ミラー維持（前回プローブ書戻し失敗時のみ）→ 再同期
    プローブ読取失敗                 → 本体ミラーで測定継続（耐性）
```
**書き戻し（電源OFF 一括）**: 変更分を **プローブ flash と 本体EEPROMミラーの両方**へ、**同じ ID・最終更新日**で書く。
- 校正/調整は運用中 RAM キャッシュに反映 → OFF時に一括書戻し（flash-busy 窓を測定中にまたがない＝統合ストア不安定の緩和）。
- 急電断で当セッション変更が失われる点は 1Wire と同性質（通常はボタンOFFの制御シャットダウンで書戻し窓を確保）。

**実装確定した細則（2026-07-25、本体 `IM_110.c` / `mainSub.c`）**:

1. **層1（既定値）に追加処理は不要**。本体 live 計算グローバルは起動時ハードコード INI（§12.1 と同値）で立ち上がるため、`mirror_ensure_valid()` は「ミラーが無効なら §12 既定を seed する」だけでよい。
2. **seed 直後のミラーは live へ適用しない**（`last_update == 0` を未同期マーカーとして扱う）。適用してしまうと、既定値が `eep_read_calhis()` で復帰した legacy EEPROM 校正を上書きし、実運用機の校正が飛ぶ。**一度でもプローブ同期または校正確定を経たミラー（`last_update != 0`）のみ**層2として live へ載せる。
3. **RPF 直後の新品プローブはプローブ採用**。ID 未割当（`probe_id == 0xFFFFFFFF`）かつ日付未設定（`last_update == 0`）のプローブは、日付比較では必ず「本体が新しい」となり RPF（新品化）の意図が再同期で打ち消される。RPF は利用者の明示操作なのでプローブを採用する。ID を `SID` で割当済みの運用機ではこの分岐に入らない。
4. **`last_update` は本体 RTC 由来の packed `YYYYMMDD`**。`Probe_SyncStore()` が書戻しのたびに更新し、プローブ flash と本体ミラーへ同一値が入る。RTC 取得不能・日付不正のときは **0 を書かず既存値を維持**する（日付の退行を防ぐ）。
5. **電源OFF 書戻しの発火判定は dirty フラグではなく突合**。「現在の live を詰めた像」と「現ミラー」をページ単位 payload（byte31 除外）で突き合わせ、差分があるときだけ書く。各 `Adj_*` に dirty フラグを配る方式は取りこぼしが出るため採らない。**変更が無ければ flash を消費しない**。
6. **ADBOAD（基板調整）も OFF 時自動書戻しの対象**（本体 Ver.0.29 で捕捉系の毎実行 flash 確定を撤去し OFF 一括へ戻したため。ADBOAD もフルハンドシェイクで `Probe_Conn_Status == 1`）。明示確定は従来どおり `AWC`/`ATCF`。
7. **IWDG に注意**。本体 IWDG は LSI32kHz/16 分周・reload 4095 = **約 2.05s** でリセットする。`RPG` 全体（3s）や `WPG`×25 はこれを超えうるため、受信待ちループと WPG ループで `WatchDog_reset()` を蹴る。

**グローバル一本化（2026-08-03 確定・本体 Ver.0.31 で実装）**:

新品化不成立バグ（`P_RPF` がプローブ flash しか既定化せず、本体 RAM/ミラーに残った調整値が
書戻しで巻き戻る）の恒久対策として、係数変数のコピー3系統を以下のモデルに一本化した。

1. **RAM 上の真実源はグローバル変数群**（`MLSS_ZR` 等の計算係数 + `Store_Probe_ID`/`Store_Last_Update`/
   `Store_ADC_Span[]`/`Store_LED_Out[]` 等の hdr/common 分）。プローブ flash と本体 EEPROM ミラーは
   その永続化先にすぎない。
2. **`probe_store_t` は直列化バッファのみ**。flash/EEPROM の読み書きの瞬間だけ一時バッファ
   （本体 `store_iobuf`）に組み、常駐像（旧 `g_hstore`/`g_hmirror`）は廃止。
3. **pack は全フィールド無条件・unpack は全フィールド展開**。旧実装の「ミラー像を土台に係数だけ
   上書き」「全0 は書かない」等の部分書きは全廃。旧イメージへの読出時サニタイズ（trzr_ship 補完・
   TR pow 既定代入・k_depth 妥当域・出荷時3点の 0 ガード）は unpack に集約し、それを通った
   グローバルに対し **unpack∘pack は恒等**（OFF 書戻しの差分判定が偽差分を出さない根拠）。
4. **新品化 = グローバル初期化のみ**（`Store_Globals_SetDefaults()` = §12.1 既定イメージを組んで
   unpack）。`P_RPF` 中継時に本体側も自動で呼ぶ。flash/ミラーへの反映は通常の電源OFF一括書戻しに
   任せる（即時書込はしない）。
5. **SetVal の本体EEPROM page12 二重持ちを撤去**。`read/write_param_common` は Meas_Mode/相関式
   選択のみ扱い、SetVal の永続化は統合ストア Page16 一本（旧 page12 の SetVal バイトは読み捨て）。

## 1. プローブ側: ADC digit → 送信 mV（①生 mV 正規化）

`IM-110_Probe/Core/Src/IM_110.c:205`（ch0-3 通常）:

```
MCP3424_AD_mV[ch] = ( digit × LSB ) × ADC_SPAN_INI / ADC_Span[ch]        （ADZero 減算は撤去, 2026-07-21）
```

- **LSB** = 0.0625 mV/digit（ch0-3, PGA=1）／ 0.03125 mV/digit（ch4, PGA=2, `:199`）
- ch4 のみオフセット項 `+ ADC_CH4_VREF`（=1000mV）が span 前に入る（`:199-200`）: `(digit×0.03125 + ADC_CH4_VREF) × ADC_SPAN_INI / ADC_Span[4]`
- 移動平均（②）: `SADA` 回で平均（既定 30）→ 本体では `ADC_mV_ave[ch]` として届く

### パラメータ（プローブ, `Core/Inc/IM_110.h`）

| 記号 | 役割 | 初期値 | 設定コマンド | 保存 |
|---|---|---|---|---|
| `ADC_Zero[ch]` | **【残置・計算未使用】** AD ゼロ点（旧オフセット。span 復活で不使用） | `ADC_ZERO_INI=0.0` | **SADZ,ch** (`:689`) | WPP→flash |
| `ADC_Span[ch]` | **AD スパン（空中の生 mV = 傾き基準）**。MLSS AD0 の能動側 | `ADC_SPAN_INI=1450.0` | **SADS,ch** (`:720`) | WPP→flash |
| `ADC_SPAN_INI` | 正規化ターゲット（空中出力の目標 mV） | **1450.0**（旧 1700, 2026-08-03 変更: 空中1700だと清水中で受光が ADC 上限 2048mV を超える個体があるため。旧々 1750/1850） | 定数 | — |
| `ADC_SPAN_S_INI` | 出荷時スパン（live 未使用） | 1850.0 | 定数 | — |
| `LED_Out[0]` | **LED PWM duty**（別機能, ALDA/手順2） | `LED_OUT_INI=0.36` | **SP,duty** (`:509`) | WPP→flash |
| `ADC_CH4_VREF` | ch4 IN- 1V オフセット補正 | 1000.0 mV | 定数 | — |

- **SADS,ch**（`:726`）【能動・MLSS AD0】: `ADC_Span[ch] = 生mV`（空中の生値, +VREF ch4）… **電圧範囲ゲートは撤廃**（2026-07-25。旧 1400〜2048mV）。**0以下のみ NG**（除数のため）。空中でこの ch を span 基準に採用 → 空中出力 = 1450mV。
- **SADZ,ch**（`:689`）【残置・計算未使用】: `ADC_Zero[ch]` を設定するが正規化式では不使用。

> 意味: プローブは「生 mV を空中基準 `ADC_Span` で割って ADC_SPAN_INI 倍」＝ **AD 値を空中 = ADC_SPAN_INI(=1450) となる傾きに正規化**して送る（原点を通る傾き調整）。
> 空中で受光が 1450mV 付近になるよう LED duty を合わせる（手順2/ALDA）のは、この正規化が素直に効く動作点にするため（LED 目標も 1450 に整合）。

---

## 2. 本体側: 生 mV(Y0, Ref) → 吸光度 ABS（③）

`IM-110/Core/Src/IM_110.c:995` `MLSS_FABSS_from_raw_mv()`:

```
adc_mv = Y0 − MLSS_ADZR                         (≤0 は 0.001 にクランプ)         :997,1000
d      = Ref − MLSS_Coef_ADZR[1]   (= Ref − refZR)                              :1003
I(0)   = MLSS_ZR + d × ( TempC[0] + d × TempC[1] )   (I0≤0 は MLSS_ZR)           :1004,1005
ABS    = log10( I(0) / adc_mv )                                                  :1006
```

仕様書 付録A（`IM-110プログラム仕様書.md:3518`）と一致:
```
ABS = Log( (MLSSZR + (現在のref出力 − refZR) × ref補正傾きB) / (Y0 − ADZR) )
```

### 用語対応（仕様書 ↔ コード）

| 仕様書 | コード（本体） | 意味 | 初期値 / 由来 |
|---|---|---|---|
| Y0 | `ADC_mV_ave[0]`（瞬時=`ADC_mV[0]`） | MLSS 受光 生 mV | プローブ送信 |
| 現在のref出力 | `ADC_mV_ave[1]` | MLSS_Ref 生 mV | プローブ送信 |
| **MLSSZR** | `MLSS_ZR` | ゼロ基準（清水/空中の分子基準 mV） | `INI_MLSS_ZR = 1850.0` |
| **ADZR** | `MLSS_ADZR` (= `MLSS_Coef_ADZR[0]`) | 受光暗時ゼロ | `INI_MLSS_ADZR = 0.0` |
| **refZR** | `MLSS_Coef_ADZR[1]` | Ref 基準（20℃清水 Ref） | 統合ストア（温度校正 ATCF が設定） |
| **ref補正傾きB** | `MLSS_Coef_TempC[0]`（B2=`[1]`） | Ref 温度補正傾き | 統合ストア（未校正=0 → I(0)=MLSSZR） |

- **温度補正が未校正**（TempC=0）なら `d×(…)=0` ⇒ `I(0)=MLSS_ZR`（= MLSSZR そのもの）。
- 上記は**現行コード**。↓ の改訂で暗時 ADZR を廃止する。

### 改訂（2026-07-20 確定）: 暗時 ADZR 廃止・オフセット2層

**暗時 ADZR（`Y0 − ADZR` の減算）を廃止**。測定式は:
```
I(0) = MLSSZR + (Ref − refZR)·(B + (Ref−refZR)·B2)     ← Ref温度補正（20℃換算, §8-T）
ABS  = log10( I(0) / Y0 )                               ← 分母は Y0 そのまま（暗時減算なし）
```
受光に効くオフセットは**2層・併用**:

| # | 名称 | 場所 | 役割 | 書換 |
|---|---|---|---|---|
| ① | **プローブ空中AD調整 (ADC_Span[], span傾き)** | 第1領域 PARAM, per-ch | Y0 を **空中→1450** に傾き正規化（AD動作点・飽和回避）。2026-07-21 に ADZero オフセット→span 傾きへ | ADBOAD/UART のみ |
| ② | **清水中オフセット = MLSSZR** | 本体 | 測定ゼロ基準（清水 Y0 = 0mg/L 点）。`ABS=log10(MLSSZR/Y0)` の分子 | 下記2系統 |

- **② MLSSZR の2系統**（＝従来抜けていた「ゼロ校正係数」を補完）:
  - **出荷時清水中オフセット**（工場, ADBOAD/UART）＝ MLSSZR 初期値。
  - **通常校正モードのゼロ校正**（現場, ユーザ）＝ 清水 Y0 を取得し **出荷時値に置き換わる値**として MLSSZR を更新。
  - No.21-30 と同構図（出荷時=初期値／校正=上書き／初期化で出荷時へ復帰）。
- **MLSSZR は固定 1700 ではなく「清水中 Y0」**（空中正規化ターゲットより受光が増え少し高い。2026-08-02 確定）。← 従来 §7 の 1850/1750 齟齬の正体。
- 清水中: `MLSSZR=Y0_清水` ⇒ `ABS=log10(Y0_清水/Y0_清水)=0` ⇒ MLSS=0。濁ると Y0↓ ⇒ ABS>0（単調増加）。
- ①(空中1450 span正規化) と ②(清水ゼロ基準) は**目的が別**ゆえ併用。SS/TR も同様（**TR は ch3=`[2]` を SS と共有**（2026-07-26 ch5 廃止）、温度補正は SS 流用）。

---

## 3. 1次演算: ABS → FABSS（④相関式 Mode_CF）

`IM_110.c:1007-1013`（6次までの多項式, ホーナーではなく明示展開）:

```
FABSS = Σ(k=0..6) MLSS_Mode_CF[MLSS_MODE][k] × ABS^k
```

- `MLSS_MODE` = 選択中の相関式テーブル index（0=No.1 … 19=No.20 … 29=No.30）
- **現状テーブルは全て「1次直線」**（`MLSS_Mode_CF[..][1]=傾き`, 他 0）⇒ 実質 `FABSS = C1 × ABS`
- **No.1-20 = FW 固定・校正不可**（等間隔グラデ, C1: No.1=21315 … No.11=42494.4572(=従来黒No.5) … No.20=61555.97）
- **No.21-30 = 校正可**（初期 C1=21315=No.1 傾き, ADBOAD 校正で統合ストアの modecf を上書き → `Apply_Coef_To_Live` で反映, `:956`）

（テーブル全体は `IM_110.c:109-143`）

---

## 4. 機差補正 kiza（⑤工場, `IM_110.c:1015` / `sel_kiza :932`）

```
FABSS ×= sel_kiza(mode=0, MLSS_MODE)
sel_kiza = MLSS_Coef_Kiza[vi]   (vi = MLSS_MODE − 20, 0.0 のときは 1.0 中立)
```

- **1 点比例**（`倍率 = 基準器 mg/L ÷ 1次演算後 mg/L`）で基準器に合わせる工場調整（design G）。
- per-eq（相関式ごと）。統合ストア保存。旧 `FABS_SPAN`（`MLSS_FABS_SPAN`）は**廃止**（式には未使用、初期 1.0 のまま残存）。

### 決定（2026-07-20）: **kiza 廃止**
- 器差（個体差）は次の 3 調整で吸収し、量産個体の数値誤差を統一する:
  1. **LED PWM**: ch1〜4 の最大値 = 1450mV
  2. **ADSpan (span傾き)**: 空気中 = 1450mV
  3. **出荷時3点調整**: ゼロ = 清水中出力（= MLSSZR ②, §2改訂）／ 2,3点 = **基準器(IM-110)の指し示す値**
- 出荷時3点調整が基準器合わせを兼ねるため、別途 1 点比例の `kiza` は**不要＝廃止**。
- measurement チェーンは `ベース → span校正` の 2 段（`×kiza` を削除）。

---

## 5. スパン校正 SP（⑥現場, `IM_110.c:1027`）

```
MLSS = MLSS_SP_A × FABSS² + MLSS_SP_B × FABSS + MLSS_SP_C
```

- 現場の 2点/3点後校正（ADCAL/MCAL）で `SP_A/B/C` を作る。機差補正の上に乗る多段（design G）。
- 初期値（`IM_110.c:93-95`）: `SP_A=0.0`, `SP_B=1.0`, `SP_C=0.0` ⇒ 未校正時 `MLSS = FABSS`。
- スパン校正係数（仕様書 `:3482`）: `SP_B = スパン校正値(mg/L) ÷ 1次演算後(mg/L)`
- 瞬時値 `MLSS_inst` も同式で `ADC_mV[0/1]`（瞬時）から算出（界面バー用, `:1029-1032`）。

---

## 6. パラメータ一覧（MLSS 計算に効くもの・保存先つき）

| パラメータ | 記号/変数 | 初期値 | 保存先 | 設定/校正手段 |
|---|---|---|---|---|
| AD ゼロ点 | `ADC_Zero[0]`(受光)/`[1]`(Ref) | 0.0 | プローブ flash(PARAM) | **SADZ**（残置・計算未使用） |
| AD スパン | `ADC_Span[0]/[1]` | 1450.0 | プローブ flash(PARAM) | **SADS**（空中1450傾き, MLSS AD0） |
| LED duty | `LED_Out[0]` | 0.36 | プローブ flash(PARAM) | **SP** / **ALDA**(手順2) |
| ゼロ基準 | `MLSS_ZR` (MLSSZR) | **1750.0**（旧1700/1850。2026-08-02 清水中受光1750へ訂正 = `INI_MLSS_ZR`） | 本体 EEPROM/live | AZC 清水ゼロ校正が実値上書き |
| 受光暗時ゼロ | `MLSS_ADZR`=`MLSS_Coef_ADZR[0]` | 0.0 | プローブ統合ストア(Page3) | **AZR**(手順3) |
| Ref 基準 | `MLSS_Coef_ADZR[1]` (refZR) | 0.0 | プローブ統合ストア(Page3) | **ATCF**(手順4) |
| Ref 温度傾き | `MLSS_Coef_TempC[0]`(B)/`[1]`(B2) | 0.0 | プローブ統合ストア(Page3) | **ATC×3 → ATCF** |
| 相関式係数 | `MLSS_Mode_CF[eq][0..6]` (No.21-30) | C1=21315 | プローブ統合ストア(Page5-9) | **AMC → AMCF** |
| 機差補正 | `MLSS_Coef_Kiza[vi]` | 0(→1.0) | プローブ統合ストア(廃止) | **AKZ**(手順5) |
| スパン校正 | `MLSS_SP_A/B/C` | 0/1/0 | 本体 EEPROM/live | 現場 ADCAL/MCAL |
| 校正設定値 | `MLSS_Cal_SetVal_1/2` | 8000/5000 | 本体 | 校正設定 |

> 統合ストアのページ割付は §12 参照（ADZR/refZR/温度補正=Page3, Mode_CF No.21-30=Page5-9, 機差=廃止）。読み書き・flash 確定は 32B ページ転送 RPG/WPG/WSC（protocol §3.2.6）に一本化。

---

## 7. 現状の不整合（要判断・修正対象の候補）

1. **【解決 2026-07-21】MLSSZR デフォルト 1850 vs 正規化ターゲット 1700 の不一致**
   - プローブ: `ADC_SPAN_INI = 1700`（送信 mV は空中 1700 基準に span 正規化, 2026-07-19 に 1850→1750, 2026-07-21 に 1750→1700）
   - 本体: `INI_MLSS_ZR / INI_SS_ZR / INI_TR_ZR = **1700**`（旧1850）へ更新済み（[IM_110.h:11/19/27](../../IM-110/Core/Inc/IM_110.h#L11)）。デフォルト==空中ターゲットの対応を復元。
   - ⇒ 電源投入デフォルトが 1700 スケールで一致。**実運用の MLSSZR は AZC(清水ゼロ校正)が空中正規化後の Y0 で上書き**（デフォルトは校正前プレースホルダ）。
   - ⚠ **残: 実機の出荷時校正係数（旧1850/1750前提で取得したもの）は 1700 スケールで取り直しが必要**（コードでなくデータ側。実機での再校正で解消）。

2. **「空中ターゲット mV」は 2 系統に登場する（用語の衝突源）**
   - **LED duty（PWM/SP/ALDA, 手順2）**: 空中受光を 1700±? に合わせる能動サーボ（別機能）。
   - **MLSS ゼロ調整（MLSSZR）**: 生 AD の分子基準。清水/空中の受光 mV を基準として取得。
   - この 2 つは別物。混同すると仕様がぶれる。

---

## 8. 基板調整 AD調整 — 改訂（2026-07-20）: プローブフラッシュ2領域で分担

プローブフラッシュ（`docs/probe-flash-map.md`）の 2 領域で役割を分ける。**SS / TR も共通の考え方**。

### プローブ／出荷時 調整の順序（2026-07-20 確定）
1. **LED PWM 調整**（明るさ・ch1〜4 max→1450）
2. **ADSpan[] 個別 span傾き**（ch1〜5 → 空中1450）
3. **Ref 温度補正**（5/20/35℃ 清水 Ref → 2次式, 20℃換算）← §8-T
4. **出荷時3点調整**（ABS→mg/L の **2次式** = No.1 基準）← 温度補正確定後に実施

理由（3→4 の順）: 出荷時3点調整の ABS は温度補正込みの `I(0)`（§2）から出るため、
**先に温度補正を確定**して各点 ABS を 20℃換算した状態で焼き込む。逆順は非20℃で焼いてしまいズレる。

### 第0ステップ: LED PWM 調整（＝1450mV 収束機能, ALDA 相当）
- **目的**: 空中で **ch1〜4 の AD が 2048mV（＝16bit AD 満スケール, 振り切り）に達しない**よう LED 出力を下げる。
- **基準**: **ch1〜4 の生 mV で最も高いものが 1450mV** になるよう LED duty を調整（比例＋収束反復のサーボ）。span 正規化ターゲットと一致（2026-07-21 に 1750→1700、2026-08-03 に 1700→1450）。
- **保存先**: `LED_Out[0]`（第1領域 PARAM）。
- これが「1450mV に収束する機能」（先に AD0 と混同していたもの。**別機能**）。
- 実装参照: 本体 [Adj_LED_Auto](../../IM-110/Core/Src/IM_110.c#L1553)（`ALDA`）。
- **対象 ch = ch1〜4（code index `[0]`〜`[3]`）**＝ MLSS受光/MLSS_Ref/SS受光/SS_Ref。**ch5(透視度 `[4]`)は除外**（§0 対応表）。


| 領域 | ページ | 保持するもの | 本 AD 調整での役割 |
|---|---|---|---|
| **第1領域 = PARAM** | Page62 (`PARAM_AD`) | `ADC_Span[]`, `LED_Out[]` 等 | **ADSpan = 空気中出力を 1450mV にする傾き基準**（①・2026-07-21 に ADZero オフセットから変更） |
| **第2領域 = 統合ストア(STORE)** | Page63 (`STORE_AD`) | 校正係数(統合ストア) | **出荷時2次式(No.1基準)**＋**出荷時清水中オフセット(MLSSZR初期値, ②)**＋Ref温度補正＋No.21-30 |

> 受光ゼロは 2 層併用（① 空中1450=ADSpan傾き / ② 清水ゼロ=MLSSZR）。詳細は §2 改訂。暗時 ADZR は廃止。

### 第1領域: ADSpan（＝画面「MLSS AD0」）
- **ADSpan** = **空気中**で受光出力を **1450mV にする傾き(span)基準**。プローブフラッシュ**第1領域(PARAM)**。空中の生 mV を `ADC_Span[ch]` に記録し、`出力=生mV×1450/ADC_Span` で正規化 → 空中 1450mV。**2026-07-21 に ADZero オフセット→span 傾きへ変更**。
- **対象 ch = ch1〜4（index `[0]`〜`[3]`）を個別に**。**ch5 は廃止につき対象外**（2026-07-26。旧: ch1〜5。`SADS,4` は NG を返す）。
- ID-200T ADZERO（[Calc.c:432](../../IM-110/Core/Src/Calc.c#L432) `calc20` case0 `ADzr[0]=R`）と類似操作だが、ID-200T は **0mV オフセット**、IM-110 は **空中1450mV への傾き正規化**。
- 取得ゲート: **電圧範囲ゲートは撤廃**（2026-07-25）。ch ごとに空中生mVが大きく異なり（実測 ch1:1725 / ch2:1069 / ch3:1339 / ch4:968）、旧ゲート 1400〜2048mV では ch1 以外が調整不能だったため。**0以下のみ NG**（`ADC_Span` は正規化の除数）。
- 画面タイトル **「MLSS AD0」**（基板調整側。校正側「MLSS 0」とは別物）。

### 廃止
- **`ADZero`（オフセット `生mV − ADC_Zero`）・`ADMiddle[]` は無し（廃止・未使用）**。
  → 2026-07-20 に一度 ADZero 一本化したが、2026-07-21 に **span 傾き正規化（`× ADC_SPAN_INI / ADC_Span`, `IM_110.c:205`）へ巻き戻し**。`ADC_Zero`/`SADZ` はコマンドのみ残置・計算未使用。
  曲線/高次項は第2領域の 4次式（出荷時3点校正）が担う。

### 第2領域: 出荷時 3 点校正係数（4次式）— プローブ調整の第3ステップ
プローブ調整（LED PWM → ADSpan）の後に実施する。

- **動作は現行「MLSS 校正モード, 相関式 No.21〜30」と全く同じ** 3 点校正機構
  （[adj_fit_poly](../../IM-110/Core/Src/IM_110.c#L1244) 多項式フィット → `*_Coef_ModeCF` → `WCM` → `WCFC` 確定）。
- **この校正機能を ADBOAD にも追加**する（現状は CAL/校正モードのみ）。**MLSS / SS / TR すべて対象**。
- **出荷時補正係数 = MLSS / SS / TR それぞれ 1 式ずつ持つ**（＝出荷初期値係数）。
  - **実質 2 次式**（3 点で一意に決まるのは 2 次まで）。格納は `modecf[7]` 枠（最大6次）だが **3 次以上は 0**。
    ＝「係数枠が 4 次まであるだけ」で実体は 2 次。
  - 経緯: 元の MLSS白 6点→4次式（付録A）は実験式。**最終的に 1 次傾き補正（相関式 No.13 ≒ ゲイン2.19 相当）に集約**したため、
    出荷時は **3点→2次式で確定**（4次は使わない）。

### 相関式 No. モデル（改訂・決定: (B) 併存）

- **相関式係数は今回から「傾きのみ（1次スケール）」に簡素化**（元は独立した 3 点調整の 4 次式係数だった）。
- **出荷時 MLSS 3 点調整係数 ＝ 相関式 No.1 相当の 4 次係数**（＝基準カーブ）。
- **No.1〜20**: この **No.1（基準 4 次式）を基準に、傾きを順次大きくしていく**（No.1=基準, No.2→No.20 で傾き増）。
  - 各相関式が持つのは **傾き（スケール）係数のみ**。基準の 4 次カーブ形状は共通で、傾きだけが No. で変わる。
- **No.21〜30**: **独立した 2 次式**（ユーザ 3 点校正で作成、基準の代わりに使う）。**傾き(ゲイン) k は 1.00 固定**
  （傾きは 2 次式係数に内包されるため追加ゲイン不要）。← (B) 併存。

**統一形（No.1〜30 共通）**:
```
出力(mg/L) = curve(ABS) × k
  No.1〜20 : curve = 出荷時2次式(基準・共通),  k = k_No（No.1=1.00 … No.20=2.8879）
  No.21〜30: curve = 各スロット独立2次式,       k = 1.00（固定）
```
**No.21〜30 の初期状態・初期化（決定 2026-07-20）**:
- **未調整（初期）状態**: 全10スロットに **出荷時3点校正係数（基準2次式）をコピー**、傾き **k=1.00**。
  ⇒ 未調整の No.21 選択でも No.1（基準×1.00）と同結果。**modecf が「全0」になる状態は通常存在しない**
  （現行の「全0=未校正フォールバック」ロジックに依存しない）。
- **相関式初期化機能**: No.21〜30 を上記初期状態（現在の出荷時係数を再コピー、k=1.00）へ戻す。
  ＝ ADBOAD/UART の新機能（既存 校正リセット C_RESET との関係は別途整理）。
- No.21-30 の運用上のセーフ機構の詳細は**未定**（今回はここまで）。
- ⚠ 現行実装は No.1-20 が FW 固定の 1 次直線（絶対傾き）テーブル（[IM_110.c:109](../../IM-110/Core/Src/IM_110.c#L109)）。
  本定義で **No.1 = 出荷時4次式、No.2-20 = その傾きスケール**に変わる。

### 傾きの掛け方（決定）＋ ゲイン係数変換表

- **決定**: 相関式 No. は **基準 4 次カーブ全体をゲイン倍**する。
  ```
  出力(mg/L) = 出荷時4次式(ABS) × k_No        （No.1: k=1.00）
  ```
- **抜けの指摘（要修正）**: 現行 `MLSS_Mode_CF[No.1-20]` は**絶対傾き**（No.1=21315…）で、ゲインモデルの
  No.1=1.00 になっていない。**No.1〜20 を「No.1=1.00 基準のゲイン係数 `k_No`」へ変換**しないと
  出荷時 4 次式（=No.1）がベースとして噛み合わず、出荷時 3 点調整が成立しない。

**ゲイン係数 `k_No`（No.1=1.00, 現行絶対傾き ÷ 21315。等間隔 step=0.099364）**:

| No. | k_No | No. | k_No | No. | k_No | No. | k_No |
|---|---|---|---|---|---|---|---|
| 1 | **1.0000** | 6 | 1.4968 | 11 | 1.9936 | 16 | 2.4905 |
| 2 | 1.0994 | 7 | 1.5962 | 12 | 2.0930 | 17 | 2.5898 |
| 3 | 1.1987 | 8 | 1.6955 | 13 | 2.1924 | 18 | 2.6892 |
| 4 | 1.2981 | 9 | 1.7949 | 14 | 2.2917 | 19 | 2.7886 |
| 5 | 1.3975 | 10 | 1.8943 | 15 | 2.3911 | 20 | **2.8879** |

- SS も同構造（現行 SS テーブルも同一傾き規則）。TR は累乗式のため別扱い（本ゲインモデル対象外）。
- **コード変更（予定・未実施）**: ① `MLSS_Mode_CF`/`SS_Mode_CF` No.1-20 を上記 `k_No` へ置換、
  ② 出力計算を `出荷時4次式(ABS) × k_No` に変更（現行の `Σ Mode_CF[k]·ABS^k` 直接評価から）。

### 統合ストア レイアウト影響
- 新設: 出荷時4次式 × 3 mode（MLSS/SS/TR 各1式）。
- 据置: `modecf[10]`(MLSS No.21-30) / `modecf[10]`(SS) / TR。
- ⇒ 統合ストア(§12)へ吸収し、protocol §3.2.6 の 32B ページ転送(RPG/WPG/WSC)で読み書きする（旧 ver2→ver3 拡張は廃止）。

### 書き換え権限（アクセス制御）
- **プローブ調整係数**（＝ **ADSpan[]（ADC_Span, span傾き）**（第1領域）＋ **LED PWM duty** ＋ **出荷時2次式**（第2領域）＋ **Ref温度補正係数**）は、
  **ADBOAD の操作、または UART デバッグコマンドでのみ書き換え可能**とする。通常のユーザ校正フローからは書き換えない。

## 8-S. SS / TR への適用

- **考え方は MLSS と共通**: ADSpan 個別（SS=ch3/ch4, **TR=ch3**（2026-07-26 ch5 廃止, SS と共有））／LED PWM（SS=ch3/ch4）／
  出荷時基準式／Ref温度補正（**TR は SS を流用**）／No.21 初期値=出荷時係数コピー。
- **SS / TR は相関式 No. が無い＝No.21 の単一式のみ**（No.1〜20 のゲイン選択なし）。
  単一の校正可能式ゆえ、**校正モードで ゼロ校正＋2点/3点校正が自然に実施可能**。

### TR(透視度) のカーブ形 — 累乗 → 2次補正（仕様書記載もれ、2026-07-20 定義）
- **出荷時ベース = 累乗式** `cm₀ = a·ABS^b`（[TR_FABSS_from_raw_mv](../../IM-110/Core/Src/IM_110.c#L1092)、AMCP=累乗フィット、a,b=TR modecf）。
- **その後に 2 次補正式**: `cm = A·cm₀² + B·cm₀ + C`（校正で作る。MLSS/SS の SP span校正と同一の2次）。
- **校正方法は MLSS と同じ**（2/3点、基準器表示値を入力してフィット）:
  例) 90cm 相当サンプル→基準器 92cm 入力=スパン点、45cm 相当→基準器 45cm 入力=中間点 → (累乗出力, 基準器cm) を2次フィット。
- 現行コードは既に `TR_FABSS(累乗) → TR_SP_A·f²+TR_SP_B·f+TR_SP_C`（[adj_read_live](../../IM-110/Core/Src/IM_110.c#L1458)）＝**実装済み構造**。

| | 出荷時ベース | 上に乗る2次補正(校正) |
|---|---|---|
| MLSS/SS | 2次式(No.1基準) × k_No | SP span校正 `A·f²+B·f+C` |
| TR | 累乗 `a·ABS^b` | 同じ2次 `A·cm₀²+B·cm₀+C` |

- **全モード共通構造**: 「ベース(2次/累乗) → 2次補正(校正)」。ベース形が違うだけで**上に乗る2次補正・校正操作は完全共通**。

## 8-T. Ref 温度補正（出荷時3点調整の前に実施）

- **機能**: **5 / 20 / 35℃ の清水中 Ref 出力**で多項式を作り、それで **MLSS・SS を常に 20℃換算出力に補正**する。
  Ref を温度プロキシに使い、運転時に温度計不要（Ref は測定 ch の1つ）。
- **算出方法**: 出荷時3点調整と**同じフィット関数** [adj_fit_poly](../../IM-110/Core/Src/IM_110.c#L1244) を流用。
  **3 点ゆえ 2 次式**（`B, B2` の2係数＋refZR）。
- **【重要】相対 mV で式を立てる**（原本仕様書に準拠）: 測定式は `I(0)=MLSSZR + B·d + B2·d²`（`d = Ref − refZR`）＝
  **相対 Ref `d`（20℃基準からの変化幅）の多項式**。よって 2 次フィットは **x=`d=Ref−Ref20`, y=`Δ受光=受光−受光20`** の
  **相対 mV・原点通過**で解く（20℃点=(0,0)、5℃/35℃の2点で B,B2 を決定）。**絶対 mV で 2 次を立ててはいけない**
  （係数が平行移動不変でないため測定式と不整合）。
  - 現行 [adj_calc_tempc](../../IM-110/Core/Src/IM_110.c#L1364) は **1 次(傾き B)** で、傾きは平行移動不変ゆえ絶対/相対同値・実質相対ベース（refZR=ref20 を別持ち）。
    2 次化の際に上記「相対 mV・原点通過」を厳守する。
- **式への反映**（§2）: `I(0) = MLSSZR + (現ref − refZR)·(B + (現ref − refZR)·B2)`。
  `refZR` = 20℃ 清水 Ref、`B=tempc[0]`、`B2=tempc[1]`。未校正(0)なら `I(0)=MLSSZR`（補正中立）。
- **TR(透視度)** は **SS の温度補正を流用**（同一光学 ch・Ref 共通、既存設計）。TR 固有の温度係数は持たない。
- **順序**: LED PWM → ADSpan → **本補正** → 出荷時3点調整（§8 順序表）。

### 命名の区別（混同禁止）
| 名称 | 側 | 実体 / 保存先 |
|---|---|---|
| **MLSS AD0** | 基板調整 | 受光 AD の 空中1450 span傾き → 第1領域 `ADC_Span[]` |
| **MLSS 0** | 校正 | 出荷時3点校正係数(4次式) → 第2領域 統合ストア `modecf[7]` |

## 10. 水深補正（2026-07-20 定義）

- **原点**: **電源ON 直後の大気圧**を原点（相対値）。自動ゼロ。現行 [Calc_Depth](../../IM-110/Core/Src/IM_110.c#L1152) で
  初回 `Depth_offset = P` として実装済み。
- **換算**: `Depth[m] = (P − P_atm0) × k_depth`。`P` = プローブ **HSPPAD143A**（PTDET, hPa）→ 本体 `ADC_mV[5]`。
- **校正**: **1 点スロープ校正**。ADBOAD スイッチ / UART デバッグコマンドで、**6m 相当の加圧時に 6.0m 表示**になるよう
  `k_depth`（傾き）を調整。オフセットは自動ゼロなので**傾きのみ**。
- **初期値**: 現行の固定 100hPa/m 相当（`k_depth = 1/100`）。現行の `Depth=(P−offset)/100.0` を `×k_depth` へ置換。
- **保存/権限**: `k_depth` はプローブ側保存、**ADBOAD/UART のみ書換**（他のプローブ調整係数と同一扱い）。

## 11. 残・実装事項（仕様は確定、作業は別途）

- **フラッシュ配置**: 現行 第1領域(PARAM)＋第2領域(係数ページ) を **1 領域に統合**する方針（次トピックで詰める）。
  併せて全プローブ調整係数の配置を確定。※ 係数格納フラッシュの通信不安定（`probe-flash-map.md` §不安定）の恒久化とも関係。
  - **32B ページ共通レコード方式（2026-07-20 決定）**: 本体EEPROM の 1 ページ=32B（7float+4B）構造を
    そのままプローブflashのデータ構造に流用し、**本体EEPROM ⇄ プローブflash をバイト単位で完全一致**させる。
    → §A-2 の同一配置ミラーを `memcpy`/ブロック転送で実現、struct 定義・シリアライズ層を共通化。
    - 成立根拠: 両 MCU とも little-endian ARM + IEEE754 単精度 → float 表現がバイト一致。
    - 共通化範囲: データ配置＋シリアライズのみ（低レベルアクセスは本体=M95256 SPI EEPROM / プローブ=内蔵Flash で別）。
    - 実装注意: 構造体は明示レイアウト（4B境界/packed）で padding 差を排除。プローブは2KBページ消去→全レコード書込（OFF時一括書戻しと整合）。
- **格納が必要な係数（統合対象の棚卸し）**:
  - ADC_Span[ch]（空中1450 span傾き, 5ch）／ LED PWM duty ／ k_depth
  - 出荷時清水オフセット = MLSSZR 初期値（mode別）／ Ref温度補正（refZR, B, B2; MLSS/SS）
  - 出荷時2次式（No.1基準, MLSS/SS）＋ TR 累乗(a,b) ／ k_No 枠(No.1-20) ／ No.21-30 独立2次式(各10)
  - 現場校正: MLSSZR上書き値 ／ SP_A/B/C（MLSS/SS/TR）
- 統合ストア(§12)へ吸収し、protocol §3.2.6 の 32B ページ転送(RPG/WPG/WSC)で読み書き（旧 ver2→ver3 拡張は廃止）。
- **実装状況（2026-07-20, ビルド確認済み・動作確認は後）**:
  - ✅ 本体計算: 暗時ADZR撤去 ／ kiza撤去 ／ MLSS No.1-20 を k_No化（`eval_modecf×k_No`, `k_No_MLSS[20]`表）／ SS/TR も暗時撤去・kiza撤去（[IM_110.c MLSS/SS/TR_FABSS]）
  - ✅ 温度補正2次化: `adj_calc_tempc` を **相対mV・原点通過の2次**へ（B/B2, §8-T）
  - ✅ プローブ: **span傾き正規化**（正規化=生mV×1450/ADC_Span, 2026-07-21 に ADZero オフセットから巻き戻し）／ **SADS=空中1450 span基準**（MLSS AD0。電圧範囲ゲートは 2026-07-25 撤廃、0以下のみ NG）／ SADZ/ADC_Zero は残置・計算未使用
  - ✅ 水深: `Calc_Depth` を `×Depth_k`（初期1/100）＋ `Depth_Calib_Span6m`＋UART `AWD6`
  - ✅ UARTコマンド追加: `AZC`(清水ゼロ→*_ZR) ／ `ACRI`(相関式初期化 No.21-30←出荷時)
  - ✅ ADBOAD 仮画面: `adj_probe()`（DO系スロット流用・タイトル据置）。MEM=実行/DISP=次。MLSS AD0/LED PWM/清水ゼロ/水深6m/相関式初期化/温度フィット/温度点5-35℃ を Adj_* へ配線。`Adj_MLSS_AD0`（**SADS** ch0-4+WPP）
  - 🔲 未実装: フラッシュ統合32B・3層同期(既存機構のまま) ／ ADBOAD 本画面(タイトル/レイアウト/値表示) ／ 出荷時3点調整のADBOAD配線(基準器値入力UI) ／ 安定判断±10%(タスクD)
- 注: 出荷時2次式データ未取得のため、No.1-20 は基準=Mode_CF[0]（現行=線形21315）で当面 従来同等の出力。出荷時3点調整(AMC/AMCF)実施後に本来の2次カーブに。

## 12. フラッシュ レコード割付（2026-07-20 確定案）

**方針**: 1 機能 = 32B ページ単位で分離。**32B 共通レコード**で プローブflash ⇄ 本体EEPROMミラー を **相対レコードN で完全一致**
（`本体EEPROM[ミラー基準+N] == プローブflash[領域先頭+N]`）。絶対ページNo.は別（本体EEPROMは header/共通設定/界面/測定履歴 等
本体固有データも持つため、ミラーは連続サブブロックに置く）。既存の本体EEPROM疎レイアウト(page19-60,2page/相関式)は本配置に**置換**。

**採用した設計判断**:
- **k_No（No.1-20 ゲイン）は全プローブ共通の固定 progression ＝ flash 非保存**（FW 定数表）。保存は「選択中 No.」のみ。
- **MLSSZR 等ゼロ基準は「出荷時」＋「現場」の 2 枠**（初期化で出荷時へ復帰）。No.21-30 は各スロットに独立2次式（初期値=出荷時ベース P4 のコピー）。

| Page | 機能 | 内容（float/32B, 特記なき限り float） | 使用/31B |
|---|---|---|---|
| 0 | ヘッダ | magic(4B)＋**Probe_ID**(4B)＋**最終更新日**(4B)＋Product_Name(7B)＋checksum(4B) | 23 |
| 1 | ADC span 正規化 | ADC_Span[ch1-5]（空中1450 傾き基準） | 20 |
| 2 | **LED PWM duty** | **LED_Out[0..4]**。`[0]`=MLSS系(ch1,2, adboad.md 4) / `[1]`=SS系(ch3,4, adboad.md 7) / `[2..4]`予備 | 20 |
| 3 | 水深係数 + プローブ設定 | k_depth ＋ SADA, SADC, 選択中相関式No.(MLSS), Meas_Mode（各1B） | 8 |
| 4 | MLSS ゼロ/温度 | MLSSZR_出荷時, MLSSZR_現場, refZR, B, B2 | 20 |
| 5 | MLSS ベース/校正 | 出荷時2次(c0,c1,c2), SP_A,SP_B,SP_C | 24 |
| 6–10 | MLSS No.21-30 | 独立2次式×10（(c0,c1,c2)を2式/ページ）＝5ページ。初期値=P5ベースのコピー | 24 |
| 11 | SS ゼロ/温度 | SSZR_出荷時, SSZR_現場, refZR, B, B2 | 20 |
| 12 | SS ベース/校正 | 出荷時2次(c0,c1,c2), SP_A,B,C | 24 |
| 13 | SS No.21 | 独立2次式×1 (c0,c1,c2) | 12 |
| 14 | TR ゼロ/ベース/校正 | TRZR, 出荷時累乗a,b, SP_A,B,C（温度補正=SS参照） | 24 |
| 15 | TR 校正後累乗 | 累乗a,b(校正後)。0,0 = 未校正マーカー | 8 |
| 16 | 校正点設定値 SetVal | MLSS/SS/TR × (SetVal1, SetVal2) | 24 |
| 17 | MLSS 出荷時3点調整 調整係数 | x[3]=各点の受光mV(ゼロ枠[0]は未使用=Page4 zr が真実源/スパン/中間), y[3]=設定値(0/8000/4000), valid=0x07固定 | 25 |
| 18 | SS 出荷時3点調整 調整係数 | 同上 (設定値 0/800/400) | 25 |
| 19 | TR 出荷時3点調整 調整係数 | 同上。[0]=清水点として使用 (x=清水mV, y=理論値100cm)、設定値 60/30 | 25 |
| 20 | MLSS 現場スパン校正 捕捉点 | x[0]=フル点 raw mV(ゼロ校正前), x[1]=中間点 raw mV, y[0..1]=校正時基準値(SetVal), valid(bit0=フル/bit1=中間) | 25 |
| 21 | SS 現場スパン校正 捕捉点 | 同上 | 25 |
| 22 | TR 現場スパン校正 捕捉点 | 同上 | 25 |
| 23 | MLSS 温度補正 捕捉点 | ref[3]=Ref mV, juko[3]=受光 mV（5/20/35℃。設定温度は固定のため保存しない）, valid(bit0-2) | 25 |
| 24 | SS 温度補正 捕捉点 | 同上（TR は SS の温度補正を参照するため枠なし） | 25 |

- **合計 25 ページ = 800B**（`STORE_NPAGE`=25）→ プローブ flash Page63(2KB) / 本体EEPROM ミラー **page19-43**
  （2026-08-02 に旧位置 page61 から移設 — 撤去済み legacy 校正領域の再割当。以降の拡張は page44-60 → 最大 page80 まで）。
- **2026-08-02 改訂 (v0.6.1)**: Page23/24 に温度補正捕捉点を新設（23→25 ページ）。`ATC` が 5/20/35℃ の
  該当スロットへ現在の Ref/受光 mV を上書き永続化し、**3 点揃っていればその場で B/B2/refZR (Page4/11) を
  再フィット** (`adj_calc_tempc` §8-T) → 即 flash 確定。単一温度点の後日取り直し可。3 点未満の間は
  捕捉保存のみで温度補正は中立既定 (B=B2=0) のまま。旧 23 ページイメージは読出時自動移行。
- **2026-08-02 改訂**: Page17-22 に調整・校正の捕捉点を新設（17→23 ページ）。
  - **出荷時3点調整 (Page17-19) — 調整係数 常設モデル (2026-08-02 ユーザー仕様確定)**:
    ゼロ/スパン/中間の 3 係数は常に存在し、新品時から既定式と自己整合する初期値
    (`STORE_DEF_ADJ3_*`) が入っている。ADBOAD 画面 16-24 (温度補正6画面挿入後の番号) の実行は**その係数だけを現在の受光mVで
    置き換え、常に 3 点で式を再計算**して基準式 (P5/P12 の c0-c2、TR は P14 累乗 a,b) と
    No.21-30 スロットを更新 → `Adj_Commit` で即 flash 確定する。どの係数をどの順に単独実行しても
    正しく 2 次式が算出できる。
    - ゼロ実行 = ZR (Page4/11 zr、出荷時経路なので ship/field 両枠) を置換 → 再計算。
      係数 x は mV で持ち ABS=log10(ZR/mV) を再計算時に導出するため、ゼロ変更後も
      スパン/中間係数はそのまま新しいゼロ条件で正しく効く。
    - スパン実行 = スパン係数を置換し、**中間係数を「ABS でスパンの 1/2」(mV=√(ZR·スパンmV))・
      設定値 1/2 にセットし直す** → (0,0)-(中間)-(スパン) が共線になり実質 1 次式。
    - 中間実行 = 中間係数のみ置換 → 2 次式 (曲線)。
    - TR は [0]=清水点 (mV, 理論値100cm) も通常係数。スパン実行時の中間セットは清水-スパン
      2 点の累乗厳密カーブ上の ABS 中点 (実質 2 点式)。再計算は 3 点の累乗フィット。
  - **現場スパン校正 (Page20-22)**: ADCAL/MCAL (後校正) で選択した測定記録の **raw mV (ゼロ校正前)** と
    校正時の基準値を点ごとに保存する (`Calib_CalPt_Store_Current` / `Calib_SpanFromPoints_Current`)。
    - **2点校正 (ADCAL)** = 明示的な直線化: フル点を記録し**中間点の捕捉を破棄** → SP_A=0/SP_C=0/SP_B=REF/1A。
      従来機 (ID-200T 系) の「中間点をゼロ〜スパンの 1/2 で算出しセットして直線化」と同一の式になる
      (直線上の合成中間点は情報を持たないため保存しない表現を採る)。
    - **中間校正 (MCAL)** = 中間点をセットして 3 点係数を再算出するだけ。フル点は保存値を使い
      MCAL 内では選ばせない。フル点未捕捉 (新品等) なら演算エラー — 先に ADCAL を実施する。
    - **ゼロ校正 (ZCAL)** = ZR (Page4/11/14 の zr_field/trzr) を現在値へ更新した直後、保存済み
      スパン捕捉点から SP をその場で再導出して即時反映し、ストアへ即書戻しする。捕捉点は raw mV
      なので新しいゼロ条件の FABSS で再計算され、ゼロ→スパンの整合が常に保たれる
      (フル点未捕捉なら SP は恒等のまま)。ゼロは Page20-22 に「点」としては持たない —
      ZR 自体がストア済みパラメータであり、それが捕捉点 raw mV→FABSS 変換の条件になる。
    - raw mV で持つため FABSS はゼロ校正・相関式の**現在条件**で再導出され、これらを変えた後の取り直しも整合する。
  - 旧レイアウト（20 ページ中間版 / 17 ページ）は読出時に `store_upgrade_legacy()` で自動移行（捕捉点なしで追加、protocol §3.2.6）。
- **2026-07-31 改訂**: 機能ごとに 1 ページを割り当てる方針へ変更。旧 Page1 が ADC_Span+LED_Out+k_depth の混載で、
  LED duty を 2 系統に増やした際に `LED_Out[]` がページをまたぐ配置になったため。Page1=ADC / Page2=LED / Page3=水深+設定
  に分離し、Page4 以降は旧 Page3 以降が +1 ずれた形。**旧レイアウトとバイト互換は無い（要 `RPF`）**。
- **相関式初期化 / ゼロ初期化** = P5/P12 の出荷時ベース・P4/P11 の ZR_出荷時 を、対応する現場枠・No.21-30 スロットへコピー。
- **LED duty は 2 系統**: adboad.md 4(MLSS LED PWM, ch1,2) が `LED_Out[0]`、7(SS LED PWM, ch3,4) が `LED_Out[1]`。
  プローブ LED 駆動は TIM3 CH1 の 1 本なので、`SADC` マスクで有効化された系統の duty を PWM へ適用する。
  `LED_Out[0..4]` は Page2 に連続配置する（ページをまたがせない）。
- **要詰め（実装時）**: Page2 の残り int パラメータの正確なバイト割付、TR の No.21 と 出荷時累乗 の関係（同一枠 or 別枠）、SS 余剰スロットの扱い。

### 12.1 新品（工場出荷前）既定値 — **単一ソース = `probe_store.h store_set_new_probe_defaults()`**

新品化（プローブ `RPF` / 本体ミラー未同期時の seed）で書き込む既定イメージ。**係数枠を 0 埋めしてはならない**:
0 埋めだと本体 `store_unpack_to_globals()` が基準カーブと ZR まで 0 上書きし、`I(0)=0 → log10(0)=−∞`、
続く `eval_modecf(全0係数, −∞)` が `0×∞=NaN` となって MLSS/SS が **NaN**（測定破綻）になる。
新品でも「空中 ≈0・清水近傍で微小値」の健全出力にするため、下表の初期係数を必ず入れる。

| ページ | フィールド | 新品既定 | 由来（本体 live INI と同値） |
|---|---|---|---|
| 0 ヘッダ | magic / probe_id / last_update / product_name | `IM2S` / `0xFFFFFFFF` / 0 / `"IM-110 "` | 新品＝ID未割当・日付未設定（旧 ver バイトは 0 固定・判定に使わない） |
| 1 共通 | adc_span[5] / led_out / k_depth | 1450.0 / 0.36 / 1.0 | `ADC_SPAN_INI`/`LED_OUT_INI` |
| 2 設定 | SADA/SADC/選択No./Meas_Mode | 0（現状 unpack/apply 非配線・中立） | — |
| 3/10 MLSS/SS ゼロ温度 | zr_ship / zr_field | **1750.0 / 1750.0** | `INI_MLSS_ZR`/`INI_SS_ZR`（`i0=0` 回避の要） |
| | refzr / b / b2 | 0 / 0 / 0 | 温度補正 中立 |
| 4/11 MLSS/SS ベース校正 | c0,c1,c2 / sp_a,sp_b,sp_c | **0,21315.0,0 / 0,1.0,0** | `*_Mode_CF[0]`(No.1傾き) / `*_SP_*`(恒等) |
| 5-9 MLSS No.21-30 (×10) | 各 (c0,c1,c2) | **0,21315.0,0** | 初期＝No.1 傾き（校正で上書き） |
| 12 SS No.21 | (c0,c1,c2) | **0,21315.0,0** | 同上 |
| 14 TR | trzr / pow_a,pow_b / sp_a,b,c | **1750.0** / 0,0 / 0,1.0,0 | ZR で `log10(0)` 回避。pow_a=0 は本体で旧多項式へフォールバック |
| 15 TR 校正後 | pow_a_cal,pow_b_cal | 0,0 | 未校正 |
| 15 SetVal | mlss 8000/5000, ss 1000/500, tr 100/50 | 上記 | `INI_*_SP_1/2`（校正時のみ使用） |

- **バイト一致契約**: プローブ flash（`store_set_defaults` → `store_set_new_probe_defaults`）と本体 EEPROM ミラー seed（`mirror_ensure_valid` → 同関数）は**同一ビルダー**で同一 512B を組む（§A-2）。値の変更は `probe_store.h` の `STORE_DEF_*` 一箇所のみ。
- **本体 live との関係**: 本体 live 計算グローバルは起動時ハードコード INI（同値）で立ち上がるため、`RPG` 前でも測定は健全。ミラー seed は「プローブ断／本体単体交換」時の 3 層フォールバック（§A-2 層2）を健全に保つための土台。

## 9. （解決済みログ）

初期の要確認は §2/§8/§8-S/§8-T/§10 で解決:
- ADSpan=空中1450 span傾き・ADZeroオフセット撤去（§A,§8, 2026-07-21） / 暗時ADZR廃止・MLSSZR=清水オフセット2層（§2改訂）
- 出荷時=2次式(3点)・No.1基準×k_No・No.21-30独立2次式（§8） / SS/TR共通・TR=累乗→2次補正（§8-S）
- Ref温度補正=2次(3点)・順序確定（§8-T） / kiza廃止（§4） / 水深=1点スロープ（§10）
