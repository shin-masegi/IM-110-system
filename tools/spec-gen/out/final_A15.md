## 6.5 校正リセット

### 確認表示
「戻る」、「決定」、日付、時刻、初期化を確認するメッセージが表示される。

### 実行
上記画面表示中に記録スイッチ(PC10)を押すと、下記の完了メッセージを表示し、6.4 設定メニューに切り替わる。この瞬間スパン、ゼロ校正係数はプログラム内の初期値に戻す。同時に淡水/海水設定を初期値の淡水に戻す。またガイダンス機能で非表示になっているものをすべて表示可能にする。

### 完了表示
「戻る」、「決定」、日付、時刻、初期化を確認するメッセージが表示される。